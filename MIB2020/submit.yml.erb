---
batch_connect:
  template: "basic"
script:
  <%- if /frisco/.match(cluster) == nil -%>
  native:
    - "-N"
    - "1"
    - "-n"
    - "<%= num_cores %>"
    - "-A"
    - "<%= my_account %>"
    - "-p"
    - "<%= my_queue %>"
  <%- end -%>
